import { styled } from "@mui/material";

export const StyledTimerActions = styled("div")({
  display: "flex",
  flexWrap: "wrap",
  justifyContent: "center",
  columnGap: "20px",
  rowGap: "20px",
});
