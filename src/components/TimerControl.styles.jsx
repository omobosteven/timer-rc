import { styled } from "@mui/material";

export const StyledTimerControl = styled("div")({
  gridArea: "input",
  justifySelf: "center",
  width: "100%",
  maxWidth: "480px",
  position: "absolute",
  bottom: 20,
});
